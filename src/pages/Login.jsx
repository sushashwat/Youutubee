/**
 * Login Page
 * -----------
 * Route: "/login" - standalone (no Header/Sidebar), matches assignment
 * spec where Sign-in opens a separate URL. Real form + JWT in Auth step.
 */
function Login() {
  return (
    <p>Login form coming in the Auth step.</p>
  )
}

export default Login